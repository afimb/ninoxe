en:
  activerecord:
    errors:
      models:
        time_table_period:
          start_must_be_before_end: end date must be after start date
        time_table_date: 
          attributes:
            date:
              taken: dupplicate date for this timetable
        stop_area:
          x_y_must_be_both_nil_or_none: x and y must both populated or none of them  
          long_lat_must_be_both_nil_or_none: longitude and latitude must both populated or none of them